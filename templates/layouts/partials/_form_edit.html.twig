<form class="col-12" id="edit-form-{{post.id}}" action="{{path("app_edit", {id: post.id})}}" method="POST">
    <input type="hidden" name="_method" value="PUT">
    <div calss="form-group">
        <label for="comment">Post:</label>
        <textarea class="form-control" rows="5" name="content" id="comment">{{post.content}}</textarea>
    </div>
    <input type="button" value="Edit" onclick="edit({{post.id}});" class="btn btn-primary mt-2 btn-block">
</form>